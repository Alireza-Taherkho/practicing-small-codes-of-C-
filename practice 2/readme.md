Question: Write a function std::string formatBinary(std::uint32_t x) that converts the value x to a binary string with separators every 8 bits (e.g. 00000001 00000010 ...). 
Use std::bitset or bitwise operations.
